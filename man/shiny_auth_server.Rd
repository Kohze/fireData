% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{shiny_auth_server}
\alias{shiny_auth_server}
\title{Create Shiny Authentication Server}
\usage{
shiny_auth_server(
  user,
  input,
  output,
  credentials = TRUE,
  goauth = TRUE,
  anonymous = TRUE,
  project_api = NULL,
  web_client_id = NULL,
  web_client_secret = NULL,
  request_uri = NULL
)
}
\arguments{
\item{user}{A reactiveValues object with a \code{Logged} field (e.g., \code{reactiveValues(Logged = FALSE)})}

\item{input}{Shiny input object}

\item{output}{Shiny output object}

\item{credentials}{Enable email/password login (default: TRUE)}

\item{goauth}{Enable Google OAuth login (default: TRUE)}

\item{anonymous}{Enable anonymous login (default: TRUE)}

\item{project_api}{Firebase API key}

\item{web_client_id}{Google OAuth client ID (required if goauth = TRUE)}

\item{web_client_secret}{Google OAuth client secret (required if goauth = TRUE)}

\item{request_uri}{OAuth redirect URI}
}
\value{
A Shiny UI element containing the login form
}
\description{
Creates a login overlay for Shiny applications with support for
email/password, Google OAuth, and anonymous authentication.
}
\examples{
\dontrun{
library(shiny)
library(fireData)

ui <- fluidPage(
  useShinyjs(),
  uiOutput("app")
)

server <- function(input, output, session) {
  USER <- reactiveValues(Logged = FALSE)

  output$app <- renderUI({
    if (!USER$Logged) {
      shiny_auth_server(
        user = USER,
        input = input,
        output = output,
        project_api = "your-api-key",
        web_client_id = "your-client-id",
        web_client_secret = "your-client-secret",
        request_uri = "http://localhost"
      )
    } else {
      # Your main app UI
      tagList(
        h1("Welcome!"),
        actionButton("logout", "Logout")
      )
    }
  })
}

shinyApp(ui, server)
}
}
