% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/realtime-db.R
\name{rtdb_get}
\alias{rtdb_get}
\title{Get Data from Realtime Database}
\usage{
rtdb_get(conn, path, shallow = FALSE, is_class = FALSE, token = NULL)
}
\arguments{
\item{conn}{Firebase connection object}

\item{path}{Database path (e.g., "users/user123")}

\item{shallow}{If TRUE, returns only keys without values (for large datasets)}

\item{is_class}{If TRUE, data is decoded as an S4 class object}

\item{token}{Authentication token (overrides connection token)}
}
\value{
The data at the specified path, or NULL if not found
}
\description{
Retrieves data from the specified path in the Realtime Database.
}
\examples{
\dontrun{
conn <- firebase_connect(project_id = "my-project", api_key = "...")

# Get all users
users <- rtdb_get(conn, "users")

# Get specific user
user <- rtdb_get(conn, "users/user123")

# Get only keys (shallow read)
keys <- rtdb_get(conn, "users", shallow = TRUE)
}
}
