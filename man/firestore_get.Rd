% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/firestore.R
\name{firestore_get}
\alias{firestore_get}
\title{Get Firestore Document}
\usage{
firestore_get(
  conn = NULL,
  collection,
  document,
  project_id = NULL,
  token = NULL
)
}
\arguments{
\item{conn}{Firebase connection object}

\item{collection}{Collection path (e.g., "users" or "users/uid/posts")}

\item{document}{Document ID}

\item{project_id}{Project ID (uses connection or config if NULL)}

\item{token}{Authentication token (uses connection if NULL)}
}
\value{
Document data as a list, or NULL if not found
}
\description{
Retrieves a single document from Cloud Firestore.
}
\examples{
\dontrun{
conn <- firebase_connect(project_id = "my-project")
conn <- firebase_set_token(conn, auth_sign_in(conn, "user@example.com", "password"))

# Get a single document
user <- firestore_get(conn, "users", "user123")
print(user$name)

# Get from subcollection
post <- firestore_get(conn, "users/user123/posts", "post456")
}
}
